<?php

namespace App\Http\Controllers;

use Illuminate\Routing\Controller as Controller;

/**
 * O BaseController foi esvaziado. A lógica de try-catch genérica foi removida
 * para permitir que o Exception Handler do Laravel lide com as exceções
 * corretamente, retornando os códigos de status HTTP apropriados.
 *
 * Os controllers agora herdam diretamente de `\Illuminate\Routing\Controller`.
 * Este arquivo pode ser removido se não houver lógica comum a ser
 * compartilhada entre os controllers no futuro.
 */
class BaseController extends Controller
{
    //
}