#--------------------------------------------------------------------------
# Configurações do Ambiente da Aplicação
#--------------------------------------------------------------------------
# CRÍTICO: Define o ambiente como 'production' para desativar o modo de depuração e otimizar a aplicação.
APP_NAME="Site de Receitas"
APP_ENV=production
APP_DEBUG=false
APP_URL=https://api.seusite.com # URL pública da sua API

# CRÍTICO: Chave de criptografia única para produção. Gere no servidor com `php artisan key:generate`.
APP_KEY=sua_chave_de_producao_gerada_aqui

APP_TIMEZONE=UTC # Mantenha UTC para consistência entre servidores.
APP_LOCALE=pt_BR # Ajuste para o idioma principal da sua aplicação.
APP_FALLBACK_LOCALE=en

# URL do seu frontend. Usada para gerar links em e-mails e callbacks de OAuth.
APP_FRONTEND_URL=https://www.seusite.com

#--------------------------------------------------------------------------
# Configurações de Log
#--------------------------------------------------------------------------
# Em produção, o log deve ser configurado para 'stack' para permitir múltiplos canais.
# O nível 'error' ou 'warning' é recomendado para não encher os logs com informações de debug.
LOG_CHANNEL=stack
LOG_LEVEL=error

#--------------------------------------------------------------------------
# Configurações do Banco de Dados
#--------------------------------------------------------------------------
# CRÍTICO: Use um banco de dados robusto como MySQL ou PostgreSQL em produção. NUNCA use SQLite.
DB_CONNECTION=mysql
DB_HOST=seu_host_de_producao
DB_PORT=3306
DB_DATABASE=seu_banco_de_producao
DB_USERNAME=seu_usuario_de_producao
DB_PASSWORD="sua_senha_forte_de_producao"

#--------------------------------------------------------------------------
# Filas (Queues)
#--------------------------------------------------------------------------
# CRÍTICO: Mude para 'redis' ou 'sqs' para processamento de tarefas em segundo plano de forma confiável.
QUEUE_CONNECTION=redis

#--------------------------------------------------------------------------
# Armazenamento de Arquivos
#--------------------------------------------------------------------------
# CRÍTICO: Use 's3' para armazenamento de arquivos em produção para escalabilidade e segurança.
FILESYSTEM_DISK=s3

#--------------------------------------------------------------------------
# Segurança (CORS e Sanctum)
#--------------------------------------------------------------------------
# CRÍTICO: Restrinja o acesso à sua API apenas ao domínio do seu frontend. NUNCA use '*'.
CORS_ALLOWED_ORIGINS=https://www.seusite.com,https://seusite.com

# Necessário para que a autenticação de SPA (Single Page Application) funcione corretamente.
SANCTUM_STATEFUL_DOMAINS=www.seusite.com,seusite.com

#--------------------------------------------------------------------------
# E-mail
#--------------------------------------------------------------------------
# Configure um serviço de e-mail transacional real (ex: AWS SES, Mailgun, Postmark).
MAIL_MAILER=ses
MAIL_FROM_ADDRESS="nao-responda@seusite.com"
MAIL_FROM_NAME="${APP_NAME}"

#--------------------------------------------------------------------------
# Credenciais de Serviços Externos (AWS, Redis, Google)
#--------------------------------------------------------------------------
# CRÍTICO: Preencha com as suas credenciais REAIS de produção.

# AWS (para S3 e, opcionalmente, SES/SQS)
AWS_ACCESS_KEY_ID=sua_chave_de_acesso_aws
AWS_SECRET_ACCESS_KEY=sua_chave_secreta_aws
AWS_DEFAULT_REGION=sua_regiao_aws # ex: us-east-1
AWS_BUCKET=seu_bucket_s3_de_producao

# Redis (para Cache e Filas)
REDIS_HOST=seu_host_redis_de_producao
REDIS_PASSWORD=sua_senha_do_redis # ou null se não houver
REDIS_PORT=6379

# Google OAuth (para Login Social)
GOOGLE_CLIENT_ID=seu_client_id_de_producao_do_google
GOOGLE_CLIENT_SECRET=seu_client_secret_de_producao_do_google
GOOGLE_REDIRECT_URI=https://api.seusite.com/api/auth/social/google/callback

#--------------------------------------------------------------------------
# Outras Configurações (geralmente podem ser mantidas)
#--------------------------------------------------------------------------
BROADCAST_CONNECTION=log
SESSION_DRIVER=database # 'database' ou 'redis' são boas opções para produção.
SESSION_LIFETIME=120
